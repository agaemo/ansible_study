[defaults]
inventory = inventory/hosts.ini
roles_path = roles
# 学習環境なのでホストキー確認を無効化
host_key_checking = False
remote_user = ansible

[privilege_escalation]
become = True
become_method = sudo
become_user = root
